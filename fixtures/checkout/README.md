# checkout fixture

Documented API consumer of `account-service`. The baseline reads `customer_id`
from an account response; the consumer-migration agent rewrites and tests an
isolated copy against `account_id`.
