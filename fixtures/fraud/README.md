# fraud fixture

Documented API consumer of `account-service`. Its risk-scoring paths exercise
multiple legacy-field reads so migration and contract-test evidence cover more
than a single replacement.
