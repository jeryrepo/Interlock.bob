"""Interlock command-line interface.

Intentionally empty of imports: `python -m interlock_cli.cli` would otherwise
re-import the module and emit a RuntimeWarning about unpredictable behaviour.
The console script entry point is declared in pyproject.toml.
"""
